# TODOs

## Projects section

- Optional tag taxonomy pages (reuse the categories pattern) for `tags`.
- Optional project images via the `ImageMod` component on cards and detail pages.
- Testimonials page

## Needs your input

- Fill the current project in `src/config/projects.json` (`current.project`) — the
      section shows a placeholder until `name` is set.
